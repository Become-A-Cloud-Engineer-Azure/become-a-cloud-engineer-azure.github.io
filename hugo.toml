baseURL = 'https://cloud-engineer-azure.educ8.se'
languageCode = 'en-us'
title = 'Azure for Cloud Engineers'
theme = "docdock"

[outputs]
home = [ "HTML", "RSS", "JSON"]

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true